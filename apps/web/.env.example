# Базовый URL API. Пусто — относительный /api (так фронт работает,
# когда статика и API отдаются с одного домена через reverse proxy).
VITE_API_URL=

# URL WebSocket. Пусто — wss://<текущий host>/ws.
VITE_WS_URL=ws://localhost:3000/ws
